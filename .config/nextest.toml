[profile.default]
# Fail fast: stop on first failure to save time during development.
fail-fast = true
# Status level: show pass/fail summary only (skip individual pass lines).
status-level = "fail"
# Final status: show only failed tests in summary.
final-status-level = "fail"

[profile.ci]
# CI: run all tests even if some fail.
fail-fast = false
# Show everything in CI for debugging.
status-level = "pass"
final-status-level = "all"
# Slow machine-shaped tests excluded from the per-push lane; the scheduled
# full-suite lane still runs them through the default profile. Guarded by a
# name-resolution test so a stale exclusion fails loudly.
default-filter = 'not test(cargo_install_exposes_only_pointbreak_executable)'

# Per-test timing report, uploaded as a CI artifact so lane cost is measured
# from recorded runs rather than scraped from job wall-clock.
[profile.ci.junit]
path = "junit.xml"

[profile.ignored-only]
# Run only ignored tests, and show all results for debugging.
# Run with `cargo nextest run --profile ignored-only --run-ignored only`.
fail-fast = false
status-level = "pass"
final-status-level = "fail"

# The inspector contract tests each spawn a real `shore inspect` server and bind
# a socket. Serialize them so concurrent server processes do not contend for
# resources under load — the source of intermittent connection resets/refusals
# seen on constrained CI runners.
[test-groups.inspect-server]
max-threads = 1

[[profile.default.overrides]]
filter = 'binary(cli_inspect_target_display) | binary(cli_inspect_dag)'
test-group = 'inspect-server'

[[profile.ci.overrides]]
filter = 'binary(cli_inspect_target_display) | binary(cli_inspect_dag)'
test-group = 'inspect-server'
