{
  "name": "pointbreak",
  "displayName": "Pointbreak",
  "publisher": "pointbreak",
  "version": "1.0.0",
  "description": "Pointbreak Review companion for Visual Studio Code.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/kevinswiber/pointbreak.git",
    "directory": "extensions/vscode"
  },
  "engines": {
    "node": ">=20",
    "vscode": "^1.74.0"
  },
  "extensionKind": ["workspace"],
  "main": "./out/extension.js",
  "activationEvents": [
    "onStartupFinished",
    "onView:pointbreak.attention",
    "onCommand:pointbreak.refreshAttention",
    "onCommand:pointbreak.capture",
    "onCommand:pointbreak.openInReview"
  ],
  "contributes": {
    "commands": [
      {
        "command": "pointbreak.refreshAttention",
        "title": "Refresh Review",
        "category": "Pointbreak"
      },
      {
        "command": "pointbreak.capture",
        "title": "Capture Current Work",
        "category": "Pointbreak"
      },
      {
        "command": "pointbreak.openInReview",
        "title": "Open in Review",
        "category": "Pointbreak"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "pointbreak",
          "title": "Pointbreak",
          "icon": "$(pulse)"
        }
      ]
    },
    "views": {
      "pointbreak": [
        {
          "id": "pointbreak.attention",
          "name": "Review"
        }
      ]
    },
    "configuration": {
      "title": "Pointbreak",
      "properties": {
        "pointbreak.binaryPath": {
          "type": "string",
          "default": "",
          "description": "Absolute path to a shore CLI binary (overrides the bundled one)."
        },
        "pointbreak.useGlobalCli": {
          "type": "boolean",
          "default": false,
          "description": "Resolve the shore CLI from PATH instead of the bundled binary."
        },
        "pointbreak.reviewUrl": {
          "type": "string",
          "scope": "resource",
          "default": "",
          "description": "Optional URL for an externally managed Pointbreak Review server for this folder. Leave empty to let the extension restore its last verified port."
        }
      }
    }
  },
  "scripts": {
    "format-check": "biome check ./src ./test",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "check": "npm run format-check && npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.1",
    "@types/node": "26.0.1",
    "@types/vscode": "1.74.0",
    "@vscode/vsce": "3.9.2",
    "esbuild": "0.28.1",
    "typescript": "6.0.3",
    "vitest": "4.1.9"
  }
}
